# 快速开始 - 3步生成视频

**版本**: 6.0 | **更新日期**: 2026-04-28

---

## 最快路径（30秒上手）

```bash
# 1. 创建项目（以图片模式为例）
python3 01_核心脚本/video_generator_pro.py --init --template news
# 输入项目名，回车，项目创建在 projects/你的项目名/

# 2. 替换素材
#   - 改文案: projects/你的项目名/01_article/文章.md
#   - 换图片: projects/你的项目名/03_images/
#   - 换水印: projects/你的项目名/watermark.png（直接替换图片即可）
#   - 加BGM:  projects/你的项目名/02_bgm/（可选）
#   - 加音效: projects/你的项目名/02_sfx/（可选，转场时自动混入）

# 3. 生成视频（所有参数从配置读取，无需长命令）
python3 01_核心脚本/video_generator_pro.py -p projects/你的项目名
# 输出: projects/你的项目名/07_final/你的项目名_YYYYMMDD_HHMMSS.mp4
```

---

## 新功能一览（v6.0）

| 功能 | 说明 | 配置/命令 |
|------|------|-----------|
| **逐句字幕** | 默认按句子拆分显示，读完一句立即切下一句 | `subtitle_mode: sentence` |
| **Web UI** | 可视化项目管理、文章编辑、图片/BGM上传、一键生成 | `python3 01_核心脚本/webui.py` |
| **增量更新** | 只重建变更的场景，大幅提速 | 自动检测，无需配置 |
| **素材预检** | 生成前检查素材完整性，提前报错 | `--skip-pre-check` 关闭 |
| **PPT导入** | 将PPT转为项目（提取图片+文字） | `--import-ppt 文件.pptx` |
| **发布文案** | 调用Kimi API自动生成多平台文案 | `--generate-copy` |
| **片头片尾** | 自动生成文字片头/片尾 | `--intro-text` / `--outro-text` |
| **音频标准化** | 响度统一到 -14 LUFS（YouTube标准） | `--normalize-audio` |
| **双版本输出** | 同时生成横屏+竖屏两个版本 | `--dual-version` |
| **并行生成** | 音频和场景均支持3并发，速度提升2-3倍 | `--no-parallel` 关闭 |
| **进度条** | 场景生成和合并转场均带实时进度 | 自动显示 |
| **插件系统** | 自定义钩子扩展功能 | `plugins/*.py` |
| **运行日志** | 每次生成保存详细日志到 `07_final/` | 自动生成 |
| **TTS重试** | 网络失败自动重试3次，递增等待 | 自动 |

---

## 方式一：图片 + AI配音（推荐新手）

### 第1步：创建文件夹

```bash
mkdir -p projects/我的视频/{01_article,03_images}
```

### 第2步：放入素材

**写文章** `projects/我的视频/01_article/文章.md`：
```markdown
# 标题

@全局:女声
@默认图: 吃饭

@新闻男: 大家好，这是第一段，用默认图吃饭。

@图:睡觉
@新闻男: 这是第二段，强制使用睡觉图片。

回到女声和默认图吃饭。
（空行后自动回到全局默认：女声 + 吃饭）
```

**放图片** 到 `projects/我的视频/03_images/`：
- `吃饭.jpg`（默认图片）
- `睡觉.png`（第二段用）

**支持中文文件名！** 直接用 `@图:吃饭` 引用。

### 第3步：运行命令

```bash
cd /Users/kingshaoshi/Desktop/claudeCode
python3 01_核心脚本/video_generator_pro.py -p projects/我的视频
```

**完成！** 视频在 `projects/我的视频/07_final/`

---

## 方式二：Web UI（推荐日常操作）

```bash
python3 01_核心脚本/webui.py
# 打开 http://127.0.0.1:7860
```

**功能**：
- 项目列表管理（创建/加载/刷新）
- 文章在线编辑（带标记语法高亮提示）
- 图片批量上传（支持多选）
- BGM 上传（支持 .mp3/.wav/.aac/.m4a）
- 可视化参数配置（JSON + 快速下拉框）
- 一键生成视频（实时日志流）
- 最新视频预览

---

## 方式三：原视频 + AI配音

### 第1步：创建文件夹

```bash
mkdir -p projects/我的视频/{01_article,04_videos}
```

### 第2步：放入素材

**写文章** `projects/我的视频/01_article/文案.md`：
```markdown
# 标题

这是视频配音文案，会被合成到视频里。
```

**放视频** 到 `projects/我的视频/04_videos/`：
- `scene_01.mp4`（你的原视频）

### 第3步：运行命令

```bash
cd /Users/kingshaoshi/Desktop/claudeCode
python3 01_核心脚本/video_generator_pro.py -p projects/我的视频
```

**完成！** 视频在 `projects/我的视频/07_final/`

---

## 核心规则速查

### 标记规范

| 标记 | 作用 | 示例 |
|------|------|------|
| `@全局:音色` | 设置全局默认音色 | `@全局:女声` |
| `@默认图:名称` | 设置全局默认图片 | `@默认图:吃饭` |
| `@图:名称` | 指定当前段落图片 | `@图:睡觉` |
| `@音色:内容` | 指定当前段落音色 | `@男声: 你好` |

### 空行重置规则

**空行后，音色和图片都回到全局默认值！**

```markdown
@全局:女声
@默认图: 吃饭

@新闻男: 第一段，新闻男 + 吃饭。

第二段，女声 + 吃饭（空行后重置）。

@图:睡觉
@云夏: 第三段，云夏 + 睡觉。

第四段，女声 + 吃饭（空行后重置）。
```

### 图片命名（支持中文！）

```
03_images/
├── 吃饭.jpg       ← @图:吃饭 或 @默认图:吃饭
├── 睡觉.png       ← @图:睡觉
├── 打豆豆.jpg     ← @图:打豆豆
└── 背景.jpg       ← @默认图:背景
```

---

## 文件夹结构速查

```
projects/我的视频/
├── 01_article/          ← 放文章（.md）
│   └── 文章.md
├── 02_bgm/              ← 放背景音乐（可选）
│   └── 背景音乐.mp3
├── 02_sfx/              ← 放转场音效（可选）
│   └── 转场音效.mp3
├── 03_images/           ← 放图片（图片模式）
│   ├── 吃饭.jpg
│   ├── 睡觉.png
│   └── 背景.jpg
├── 04_videos/           ← 放原视频（视频模式）
│   └── scene_01.mp4
├── 05_audio/            ← 自动生成AI配音
│   ├── scene_01.mp3
│   ├── scene_02.mp3
│   └── scene_03.mp3
├── 06_scenes/           ← 自动生成场景片段（断点续传）
│   ├── scene_01.mp4
│   ├── scene_02.mp4
│   └── scene_03.mp4
│   └── .build_manifest.json  ← 增量更新记录
├── 07_final/            ← 最终合并视频
│   └── 我的视频_20260427_123000.mp4
│   └── generate_20260427_123000.log  ← 运行日志
├── watermark.png        ← 水印图片
├── .video_config.json   ← 项目配置（所有参数）
└── 参数说明.md           ← 参数文档（自动生成）
```

**规则**：有 `04_videos/` = 视频模式，有 `03_images/` = 图片模式

---

## 配置优先（推荐）

**无需长命令行！** 所有参数写进 `.video_config.json`：

```json
{
  "subtitle": true,
  "subtitle_style": "youtube",
  "subtitle_animation": "slide_up",
  "subtitle_mode": "sentence",
  "scene_fade": 0.3,
  "transition": "pixelize",
  "transition_duration": 0.8,
  "watermark": "watermark.png",
  "watermark_position": "bottom-right",
  "bgm_volume": 0.25,
  "sfx": false,
  "rate": "+18%",
  "dual_version": false
}
```

改完配置，直接运行：
```bash
python3 01_核心脚本/video_generator_pro.py -p projects/我的视频
```

临时覆盖（优先级高于配置）：
```bash
python3 01_核心脚本/video_generator_pro.py -p projects/我的视频 \
    --transition fade \
    --scene-fade 0.5
```

---

## 常见问题

### Q: 为什么空行后音色变了？
**A**: 空行会重置音色为 `@全局` 设置的默认值。如果不想重置，把内容放在同一行，不要加空行。

### Q: 支持中文图片名吗？
**A**: 支持！`吃饭.jpg` 可以用 `@图:吃饭` 引用。

### Q: Windows 创建的 .md 文件能用吗？
**A**: 支持！自动处理 Windows CRLF 换行符。

### Q: 修改了水印/字幕配置但没生效？
**A**: `06_scenes/` 下的旧场景文件不带新效果，删除它们重新生成：
```bash
rm projects/我的视频/06_scenes/*.mp4
```

### Q: 如何关闭字幕？
**A**: 改 `.video_config.json` 里的 `"subtitle": false`，或命令行：
```bash
python3 01_核心脚本/video_generator_pro.py -p projects/我的视频 --no-subtitle
```

### Q: 逐句字幕和整段字幕怎么切换？
**A**: 改 `.video_config.json`：
```json
{"subtitle_mode": "sentence"}  // 逐句显示（默认）
{"subtitle_mode": "full"}      // 整段显示
```

### Q: Web UI 启动报错 localhost 503？
**A**: 已在代码中自动设置 `no_proxy`，如仍报错检查系统代理设置：
```bash
export no_proxy=localhost,127.0.0.1
python3 01_核心脚本/webui.py
```
